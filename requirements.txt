# LLM provider SDKs (install all three so the project can switch via config)
anthropic>=0.40.0
openai>=1.50.0
google-genai>=0.3.0

# Core dependencies
pyyaml>=6.0
numpy>=1.24.0
networkx>=3.0
libclang>=16.0.0
requests>=2.31.0

# Local LineVul attention scorer (CPU) — see src/pre_phase/linevul_scorer.py
# torch CPU wheels are large (~750MB); installed once into the Docker image.
torch>=2.0.0
transformers>=4.40.0
gdown>=5.0.0
