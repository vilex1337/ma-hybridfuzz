# LLM provider SDKs (install all three so the project can switch via config)
anthropic>=0.40.0
openai>=1.50.0
google-genai>=0.3.0

# Core dependencies
pyyaml>=6.0
numpy>=1.24.0
networkx>=3.0
libclang>=16.0.0
requests>=2.31.0

# NOTE: LineVul (torch/transformers) is intentionally NOT in this file — it is
# heavy (~750MB) and only needed for attention scoring in the pre-phase. To keep
# the fuzzer Docker image light, LineVul now runs as a server on the VM HOST.
# On the host:  pip install -r requirements-linevul.txt
#               ./scripts/run_linevul_server.sh
# See src/pre_phase/linevul_scorer.py (remote path) and inference/linevul_server.py.
