# LineVul attention-distance server dependencies (runs on the VM HOST, OUTSIDE
# Docker). Kept separate from requirements.txt so the fuzzer image stays light —
# only the host that runs ./scripts/run_linevul_server.sh needs this heavy stack.
#
#   pip install -r requirements-linevul.txt
#   ./scripts/run_linevul_server.sh
#
# The in-container orchestrator reaches the server via
# MA_LINEVUL_SERVER_URL=http://host.docker.internal:<port> (set by run_benchmark.sh).

torch>=2.0.0
transformers>=4.40.0
gdown>=5.0.0
flask>=3.0.0

# light deps the server imports from the project source
numpy>=1.24.0
requests>=2.31.0
